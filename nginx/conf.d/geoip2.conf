# dynamic module
# /etc/nginx/modules/geoip2.conf

#map $http_x_forwarded_for $clientip {
#  "~^([^,]+)"   $1;
#  default       $remote_addr;
#}

#geoip2 /usr/local/share/data/GeoLite2-City.mmdb {
#  #auto_reload 5m;
#  $geoip2_iso_code default=IANA source=$clientip country iso_code;
#  $geoip2_country_name default=UNKNOWN source=$clientip country names zh-CN;
#  $geoip2_city_name default=UNKNOWN source=$clientip city names zh-CN;
#}
